# 03 — Decisions

This run was non-interactive. The orchestrator's instruction settled it in advance: re-vendor the
whole payload, roll every live bundled-version reference, write this bundle, and correct the stale
`tests/wow_mock.lua` comment. No interview was held. As instructed, no issue was filed and nothing
was pushed.

| Candidate | Outcome | Basis | Record |
|---|---|---|---|
| Options minor 17, the first-show font preload | **taken as it arrives** | It needs no host change; the addon's one font dropdown is reached through `H.SetRenderer` pages | `docs/smoke-tests.md` §11a step 6a, the first-open check |
| Slash minor 9, the `count` docstrings | **nothing to adopt** | Docstrings only, on a path this addon does not take | None. It is not a decline, so no issue is owed |
| Kit revision 18, the AceDB fake's copy key | **nothing to adopt** | The harness overrides the kit's AceDB with its own, which already fires the source key | The `tests/wow_mock.lua:356` comment, corrected |
| Pool | settled, unused | The premise is unchanged at Pool minor 3 | None |
