# 05 - Summary: LibKa0s v1.55.0 -> v1.56.0

## The move

Tag `v1.55.0` -> `v1.56.0` (`514fc0a`), base taken from the `CLAUDE.md` provenance line and
confirmed against the last payload commit (`34813a4`). Re-vendored in the RV-CM commit that carries
this bundle. Fifteen files moved a LibStub minor, none was added or removed, and no `NEEDS_*`
floor rose (the per-file table is `01_DELTA.md` 3b/3c). Test kit revision 25 -> 26.

No base correction: the v1.55.0 bundle states the right base (Step 0). No span bundle was written
here: the listing (30 tags, `01_DELTA.md` 3h) is handed to plan item CM-28.

## Delivered for free (class A)

- **Options**: `CreateOptionsPanel` parks in combat and replays at `PLAYER_REGEN_ENABLED` (not
  reached while this addon keeps its own park); `OpenOptionsPanel` answers a boolean.
- **OptionsTabs minor 4**: a banner or header page no longer leaks a widget and a texture per
  render. `settings/StatPriority.lua:702` draws a `PageBanner`, so the Stat Priority page stops
  growing the frame count on every subject switch.
- **OptionsWidgets minor 31**: the slider and color-picker drag throttles keep their own armed flag.
- **Widgets minor 10**: `ReorderList` polls on the ghost frame and pools its line (the priority rows'
  drag; its in-client check is plan item CM-29).
- **Slash minor 15**: `CliSet` / `CliReset` print a write-seam refusal (this addon's seam answers
  none, so no visible change).
- **Launcher minor 2**: the missing-library notice prints once, without the `[LibKa0s] ` prefix.
- **DebugLog minor 13**, **Perf minor 13**, **Media minor 4**, **Item minor 2**, **Bus minor 2**,
  **Lifecycle minor 2**, **Core minor 8** (`printer.Format` survives a secret in a numeric slot):
  fixes that need no host change.
- **Kit revision 26**: `asserts.lua` (`Kit.assertErrorMatches`, `Kit.assertLibraryConstant`),
  `mock_events.lua` (a recording `EventRegistry`, `__badEvents`), frames created shown, the AceDB
  fake raising and stripping defaults as AceDB-3.0 does, the lone-CR `test_eol` case, the prose gate
  reading the store-root files, section-sign case names, and the runner's `performance-§12` skip.

## Contract blockers

None (`01_DELTA.md` 3g). The one red the library's dry run predicted for this addon, the prose gate,
was cleared by CM-01 (`c95b4c6`) ahead of this commit.

## Adopted

Nothing in this run. Adoption decisions are this addon's M3 plan items: CM-05 (Options park),
CM-09 (Launcher `isEnabled` / `disabledLine`), CM-15 (Core `SafeRegisterEvent`), CM-17 (Schema,
ConsumableMaster#39), CM-18 (`Kit.assertLibraryConstant` on the Slash stub), CM-20
(`RenderTabbedSchema` `opts.tabs`), and CM-02 (the local AceDB fake brought in line with kit 26).

## Declined

None in this run.

## Skipped or unreached

- Steps 5-8 (candidates, interview, adoption, issue filing): by the plan, owned by the M3 items
  above.
- Step 3h's span bundle: plan item CM-28.
- `docs/test-cases.md` and the README test badge: regenerated by plan item CM-DOCS, never by the
  re-vendor commit.

## Suite results

Every run from the repo root through `ka0s-bounded`. Tests are passed / failed / skipped; lint is
warnings / errors.

| Gate | Headless tests | Lint | Complexity |
|---|---|---|---|
| Before the copy (v1.55.0, kit 25) | 998 / 0 / 0 | 0 / 0 in 116 files | not run |
| After the copy (v1.56.0, kit 26) | 998 / 0 / 0 | 0 / 0 in 116 files | no function above CCN 15 |
