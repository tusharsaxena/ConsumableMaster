# Candidates (ConsumableMaster)

No candidate. v1.62.0 adds no member, descriptor field, row field or major (CHANGELOG v1.62.0,
and the unchanged `members` array in `docs/api/Options/members-26.1.32.1.1.6.1.7.4.1.json`).

Delivered on the copy alone (class A, not offered):

| Surface | What arrives |
|---|---|
| Options peels (OptionsRegistry, OptionsIds, OptionsIdList, OptionsCombat minor 1) | The same members in the same order, from files under the 1500-line cap. |
| Kit revision 30 (`run-automated-tests.sh`) | An empty watch-list table in `RESULTS.md` prints `None.` (ATS-20). |
| Kit revision 31 (`run-automated-tests.sh`, `Kit.__layoutCapCovers`) | Files in `Kit.layoutCap.exempt` leave `RESULTS.md`'s band table (ATS-21). |
| Kit revisions 28, 29 | `framework.lua` and `test_prose.lua` peels; no case name or behavior changes. |
