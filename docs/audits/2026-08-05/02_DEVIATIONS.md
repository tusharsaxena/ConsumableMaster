# 02 — Deviations

**Addon:** Ka0s Consumable Master · **Prefix:** `CM-` · **Audit:** 2026-08-05 · **Standard:** **v2.21.0 (2026-08-04)**

IDs are **stable**. `CM-01…CM-29`, `CM-31…CM-33` (closed in the 2026-07-12 / 2026-07-18 / 2026-08-04
runs) are not re-listed. `CM-47` closes this run. Everything else from 2026-08-04 that is still true
keeps its ID and its wording where nothing moved. New IDs start at **CM-58**; retired IDs are never
re-purposed. Every row's `file:line` evidence is in `03_EVIDENCE.md`. Severity is the strongest
marker the violated rule carries.

Two checks could not be run and are recorded as **unverified**, not as passes: the two `diff -r`
vendor-drift comparisons against `../LibKa0s` (single-repo constraint on this session). They are
listed at the foot of this file so they are not mistaken for clean.

---

## MUST

| ID | Section (`filename-§N`) | Sev | Deviation | Fix direction |
|----|-------------------------|-----|-----------|---------------|
| **CM-30** | `localization-§4`, anti-pattern #37 | MUST | **Tracked / accepted, carried forward.** `core/TooltipCache.lua` parses **English tooltip text** for heal/mana/stat magnitudes, duration tokens, max-level caps and the `Augment Rune` marker, so magnitude detection is enUS-only. Classification itself already keys on locale-independent `classID`/`subClassID`. | No code change expected now. Documented as intentional at `docs/scope.md:20` and in the README's English-clients note. Full tooltip-text localization is a planned release. Re-affirm or schedule. |
| **CM-34** | `testing-§1`, anti-pattern #47 | MUST | **The test harness is hand-rolled while the vendored kit sits unused.** `tests/run.lua:21` does `require("harness")`; `tests/harness.lua`, `tests/loader.lua` and the 623-LOC `tests/wow_mock.lua` are addon-authored forks of `tests/_kit/framework.lua`, `loader.lua` and `mock_base.lua`, which nothing loads. `tests/harness.lua:125-133` records a drift already hand-patched back from the kit. | Migrate `tests/run.lua` onto `Kit`: `dofile` the kit's `framework.lua` + `loader.lua`, publish via `Kit.expose` under the repo's existing global name, hand the suite list to `Kit.run`. Rewrite `tests/wow_mock.lua` as `local base = dofile("tests/_kit/mock_base.lua")` plus per-key overwrites. Delete `tests/harness.lua` and `tests/loader.lua`. Suites need no edits if `Kit.expose` keeps the same global. |
| **CM-35** | `testing-§9` | MUST | **The load list most suites use is hand-maintained, not TOC-derived.** `tests/loader.lua:79-113` carries `L.PURE_LAYER`, a 33-entry hand-copied file list that `loadPure`/`loadPureNoLibs` and nearly every suite load through. Only `loadFullAddon` derives from the TOC. A file added to the TOC and forgotten here is silently untested; a renamed one is skipped, not failed. | Derive from `Loader.tocFiles("ConsumableMaster.toc")` and express the pure layer as a **filter** over that derivation rather than a second list. Pin the derivation with cases: exact TOC files in TOC order, every derived path exists on disk, no `libs/` path leaked in. Depends on CM-34. |
| **CM-36** | `options-ui-§1` | MUST | **The host member is a fresh table copying members across, not the library instance.** `KCM.Settings.Helpers` is created as `{}` at `settings/Panel.lua:41-42`; the instance is a separate local `UI`, and ~10 members are copied onto `Helpers` (`:274-280`, `:351`, `:361`, `:383`, `:458`, `:519`, `:527`, `:571-572`). The rule is explicit: the host member **MUST *be*** the instance, decorated in place. | `KCM.Settings.Helpers = optionsLib:New(descriptor)`, decorated in place with the host-only pieces (`Get`, `Set`, `Resolve`, `FindSchema`, `ValidateSchema`, `SetAndRefresh`, `CreatePanel`, `Button`, `ButtonPair`, `Label`, `LSMValues`, `EnumValues`, `BuildAboutContent`). Keep the deliberate `Section` wrapper but hang it on the instance. Land with CM-46 and CM-58 — same lines. |
| **CM-37** | `options-ui-§8` | MUST | **Two of the library's layout constants are restated as host locals.** `settings/Panel.lua:74-75` declares `local SECTION_HEADING_H = 26` and `local BUTTON_PAIR_REL = 0.492`; used at `:507` and `:711`. A host copy of a shared constant is the copy that goes stale. | Delete both locals and read them off the instance — `Helpers.BUTTON_PAIR_REL`, `Helpers.SECTION_HEADING_H` (naturally available once CM-36 lands). If the library does not expose one, add it upstream as an **additive** field rather than keeping the copy. |
| **CM-38** | `performance-§9`, `performance-§2` | MUST | **No offline scenario runner.** `tests/perf.lua` does not exist, so the mandated **zero-overhead scenario** — the *required evidence* that the gated bracket allocates nothing when capture is off — has never been produced, and every bundle records `perf` as a permanent `skip`. | Add `tests/perf.lua`, run as `lua tests/perf.lua`, **outside** the green gate and not run by `tests/run.lua`. Assert only on deterministic quantities (call counts, bytes per iteration, full collect either side). Ship at minimum the zero-overhead scenario over `KCM.Pipeline` recompute and the macro-bar cooldown repaint. Derive its load list from the TOC (testing-§9). |
| **CM-39** | `performance-§2`, anti-pattern #43 | MUST | **The bracket does an `NS` lookup per call instead of a load-time upvalue.** `core/ConsumableMaster.lua:332-333` and `modules/MacroBar.lua:322-323` both do `local perf = KCM.Perf` **inside** the timed function, then `(perf and perf.on) and debugprofilestop()`. The mandated shape is a file-scope `local Perf = NS.Perf`. The extra lookup and nil-guard are paid on the cooldown repaint, which rides `SPELL_UPDATE_COOLDOWN`/`BAG_UPDATE_COOLDOWN` at near-frame frequency. | Hoist to a file-scope `local Perf = KCM.Perf` in both files and use `local t0 = Perf.on and debugprofilestop()` / `if t0 then Perf.Note(key, debugprofilestop() - t0) end`. **Requires CM-45 first** — the upvalue is only safe once `PerfSetup` loads before its bracket sites. |
| **CM-40** | `performance-§3`, `testing-§8` | MUST | **No test pins that each declared bucket is reached by a real bracket.** `modules/PerfSetup.lua:98-101` declares `cooldown` and `recompute`; `tests/test_perfsetup.lua:141-144` only asserts *no* bucket accrues while idle. A declared bucket no bracket reaches is a lie in every report and nothing else catches it. | Add one case per bucket driving its genuine entry point with capture on — the recompute pipeline for `recompute`, a cooldown repaint for `cooldown` — and assert the bucket accrued. Iterate the descriptor's declared bucket list so a bucket added later fails until it is driven. |
| **CM-41** | `performance-§5`, `lint` | MUST | **`ConsumableMasterPerfDB` is not declared in `.luacheckrc` `globals`.** `.luacheckrc:30-34` declares `ConsumableMasterDB`, `StaticPopupDialogs`, `UISpecialFrames` only. The rule requires the diagnostics ring to be declared with a comment like the addon's own SV global. Lint is green today only because no addon file writes the global directly (the library does). | Add `"ConsumableMasterPerfDB",  -- the perf capture ring (savedvariables-§4, performance-§5)` to `globals`. |
| **CM-42** | `documentation-§3` | MUST | **`docs/performance.md` is missing.** One of the five *required* topic-detail docs: which hot paths are bracketed and why, how to run `/cm perf`, how to read the report, what the harness can and cannot resolve. | Write `docs/performance.md`. Point at the library for the shared protocol and record contract rather than restating them; do restate the addon-specific part — that the expensive paths (flyout rebuild, `MB.Update`, macro writes) are deliberately out of combat and will never appear in a capture, which `modules/PerfSetup.lua:8-15` already explains well and should be lifted. |
| **CM-43** | `documentation-§3`, `performance-§8`, `automated-tests-§7` | MUST | **`docs/perf-runs/` and its `README.md` are missing.** The standing **in-game** capture store and its doc (naming convention `<YYYY-MM-DD>-<source>-<label>.json`, schema summary, pointer to the library's canonical contract, and the note that offline runs live in `docs/automated-tests/`) do not exist. `docs/automated-tests/README.md:42-45` already says so in prose. | Create `docs/perf-runs/README.md` with the naming convention, a schema summary, the library pointer and the offline/in-game split. Commit the first real in-game capture alongside it. |
| **CM-44** | `debug-logging-§1`, `layout-§1` | MUST | **The DebugLog setup file is at `modules/DebugLog.lua`, not `core/DebugLogSetup.lua`.** The section names the file and the folder as part of the wiring MUST. Its *content* is correct — descriptor, forwarders, stub — but it sits among the feature modules. | `git mv modules/DebugLog.lua core/DebugLogSetup.lua`; move its TOC line into `# Core`, after `core/Constants.lua`, `core/State.lua` and `core/CoreSetup.lua`, before every module that calls the sink. Update `tests/loader.lua`'s path list (or the derivation, post-CM-35) and the docs that name it (`docs/file-index.md`, `docs/scope.md`, `docs/ARCHITECTURE.md`). |
| **CM-45** | `performance-§1`, `layout-§1` | MUST | **The Perf setup file is at `modules/PerfSetup.lua`, not `core/PerfSetup.lua`, and it loads *after* both its bracket sites.** `performance-§1` requires it in `core/` **and** positioned in the TOC before any module taking `NS.Perf` as a load-time upvalue; `toc-file-§5`'s own template shows it there. Today `core/ConsumableMaster.lua` and `modules/MacroBar.lua` load before it, which is exactly why CM-39's brackets re-look-up `KCM.Perf` per call. | `git mv modules/PerfSetup.lua core/PerfSetup.lua`; move its TOC line into `# Core`, after `core/CoreSetup.lua` and the DebugLog setup, **before** `core/ConsumableMaster.lua`. Then land CM-39. The `showLog`/`log` hooks are already call-time closures, so they should survive the move — verify. |
| **CM-46** | `options-ui-§1`, `layout-§1` | MUST | **The Options seam has no file of its own.** `LibKa0s-Options-1.0` is resolved and instantiated inside the 950-line `settings/Panel.lua` (`:186-252`). The section requires `settings/OptionsSetup.lua`, TOC-positioned after the schema/slash files it reads and before every `settings/<page>.lua`. | Peel the seam — the `LibStub` lookup, the descriptor, the instance publication and the lib-absent branch — into `settings/OptionsSetup.lua`, TOC-listed immediately before `settings/Panel.lua`. `Panel.lua` then keeps the schema half, `CreatePanel`, the host helpers and the registration bootstrap. Do this **together with CM-36 and CM-58** — same lines. |
| **CM-48** | `savedvariables-§2` | MUST | **Profile defaults are not declared in `defaults/Profile.lua`.** `KCM.dbDefaults` is declared inline at `core/ConsumableMaster.lua:25-…`, and `defaults/` holds only the data tables. The rule makes `defaults/Profile.lua` the **only** place a default value is hardcoded. | Move the `KCM.dbDefaults` table to `defaults/Profile.lua` (with a `defaults/Global.lua` for the `global` sub-table if worth separating), TOC-listed first in `# Defaults`. `core/Database.lua`, `core/SlashCommands.lua:713` and `settings/MacroBar.lua:21-22` already read it off the namespace, so the move is mechanical — but it changes load order, so re-check that nothing in `core/` reads `KCM.dbDefaults` at file load. |
| **CM-49** | `layout-§1` | MUST | **The `core/` load order does not begin `Compat → Constants → Namespace`.** The TOC loads `core/Namespace.lua → core/ConsumableMaster.lua → core/Bus.lua → core/Constants.lua → core/CoreSetup.lua → core/Compat.lua → …`. Scoped to the *within-`core/`* ordering only — the addon's `Modules → Settings` block order follows `toc-file-§5`, which is right. | Two legitimate outcomes, user's call: (a) reorder `core/` to `Compat → Constants → Namespace → …`, verifying that `Compat.lua` and `Constants.lua` tolerate running before `Namespace.lua` bootstraps `NS`; or (b) **raise it upstream** as a standard correction (the mandated order arguably cannot be satisfied by any addon whose `Namespace.lua` bootstraps `NS`) and record it here as an accepted deviation with the TOC's existing comments as its rationale. |
| **CM-50** | `documentation-§2` | MUST | **Root `CLAUDE.md` H1 is `# CLAUDE.md`, not `# CLAUDE.md — Ka0s Consumable Master`.** `documentation-§2` item 1 pins the title form. Everything else in the stub — adherence line, `## Standards compliance (read first)`, the docs pointer list, the green-gate line — is present and correct. | One-line edit: `# CLAUDE.md — Ka0s Consumable Master`. |
| **CM-58** | `options-ui-§1`, `performance-§1` | MUST | **The Options degradation branch leaves two runtime members `nil`, and both are called unconditionally.** `settings/Panel.lua:571-572` binds `Helpers.RefreshAllPanels`/`Helpers.RefreshScalars` to `UI and UI.<member>`; with LibKa0s absent both are `nil`. `Helpers.SetAndRefresh` calls `Helpers.RefreshScalars()` bare at `:643` — *after* the write and the `onChange` — and `O.Refresh` calls `Helpers.RefreshAllPanels()` bare at `:833`, reached from the bus receiver at `:940-943` that the recompute pipeline publishes on (`core/ConsumableMaster.lua:313`). **This is not the documented load-completing exception:** that exception covers members page files call inside schema-row literals *at file load*. These two are call-time, so the rule that applies is the ordinary one — everything else is a no-op or one honest line. Reproduced by the 2026-08-05 review as F-001 / F-002. | In the lib-absent branch publish `Helpers.RefreshAllPanels = function() end` and `Helpers.RefreshScalars = function() end` (no-ops — there is no panel to refresh and the notice is already said once at `:265-271`). Then add a degraded-load case per `testing-§8`: load the addon with `LibKa0s` omitted from the file list and drive `/cm set` and a pipeline recompute end to end, rather than hand-stubbing `Helpers`. Land with CM-36 / CM-46. |
| **CM-59** | `documentation` | MUST | **The repo root ships a fourth doc.** `documentation` opens with "Root of the repo ships exactly three docs plus `LICENSE`, and never a fourth doc" — `README.md`, `CLAUDE.md`, `DEPENDENCIES.md`. This repo also ships root `CHANGELOG.md`. Its content overlaps the README's `## Version History`, which is the standard's home for the same information. | Move `CHANGELOG.md` under `docs/` (as a topic-detail doc) or fold its player-facing rows into `README.md` `## Version History` and delete it. Whichever way, add it to `.pkgmeta`'s ignore list on the way past (see CM-56). |
| **CM-60** | `documentation-§3` | MUST | **`docs/ARCHITECTURE.md` is missing five of its eight mandated sections.** `documentation-§3` names them: Overview, Module Map, **Settings Schema**, Message Bus, **Slash Commands (table from `NS.COMMANDS`)**, **Event Subscriptions**, **Taint Notes**, **Known Limitations**. Present today: Overview (`## What it does`), Module Map (`## Layout` + `## Subsystems at a glance`), Message Bus (`## Message-bus catalog`). The other five have no section — taint is one bullet inside `## Invariants worth not breaking:87`, and there is no event table anywhere. | Add the five sections. Slash Commands and Settings Schema are both generatable from the code that already exists (`KCM.COMMANDS` in `settings/Slash.lua:65-172`; `KCM.Settings.Schema` rows across `settings/*.lua`), so keep them tables, not prose. Event Subscriptions reads off `KCM:OnEnable`. Taint Notes can absorb the invariants bullet plus `docs/midnight-quirks.md`'s secret-value rules by reference. Known Limitations should name the enUS tooltip parsing (CM-30) so the accepted deviation is visible where an engineer lands. |
| **CM-61** | `automated-tests-§3`, `documentation-§5` | MUST | **The release gate is stated nowhere; the docs still carry only the pre-v2.21.0 position.** `docs/testing.md:146-157` and `docs/automated-tests/README.md:19-33` both table `complexity` and `perf` as "no — recorded only" and then say they are "never used to fail a run", with `testing.md:156-157` adding "Commits are gated on lint + tests only" — and stopping there. `CLAUDE.md`'s automated-test paragraph goes further and says outright "It is a **report, not a gate**". As of v2.21.0 the **tag** is gated on all four suites at `pass` **plus `suites.complexity.warnings == 0`**, evaluated by the release command from the run's `manifest.json`, with a `skip` blocking as NOT EVALUATED. The commit half of every one of these sentences is still correct; the release half is missing, and the `CLAUDE.md` phrasing now reads as a contradiction of the standard. | In all three files, keep the commit-gate wording verbatim and add the release gate beside it: the two checkpoints are deliberately different — commits on the two deterministic suites, the tag on all four plus zero CCN > 15 — the runner's exit code is unchanged, and the one narrow exception is `perf` skipped for a repo that ships no `tests/perf.lua`, which **MUST** be stated in the release notes (and does apply here until CM-38 lands). Reword `CLAUDE.md`'s "report, not a gate" to "not a **commit** gate". |
| **CM-62** | `automated-tests-§2` | MUST | **The vendored runner is committed without its executable bit.** `git ls-files -s tests/_kit/run-automated-tests.sh` reports mode **100644**. `automated-tests-§2` closes with the reason: `cp` does not reliably carry the executable bit, so re-vendoring ends with `chmod +x`. On a fresh checkout `tests/_kit/run-automated-tests.sh` is not directly runnable, and every doc in the repo invokes it that way (`docs/testing.md:137-138`, `docs/automated-tests/README.md:11-12`). | `git update-index --chmod=+x tests/_kit/run-automated-tests.sh` and commit. Add the `chmod +x` step to the re-vendor procedure in `docs/testing.md` so the next re-vendor does not silently drop it again. |
| **CM-63** | `compat` | MUST | **The legacy item APIs are called directly, outside `Compat`.** `core/Compat.lua` wraps the spec APIs, `issecretvalue` and spell-name lookup — six functions, 83 lines — and nothing else. Five files reach the legacy globals directly: `core/TooltipCache.lua:459` (bare and **unguarded**), `modules/Ranker.lua:88` (bare and unguarded, with no `C_Item` preference at all), `core/Classifier.lua:169` and `core/WeaponSlots.lua:36` (guarded fallbacks after `C_Item.GetItemInfoInstant`), `modules/KCMItemRow.lua:88-89,131-132,216` (`_G.`-guarded). The addon's own `.luacheckrc:74` comment already claims these are "legacy globals wrapped by `core/Compat.lua`" — they are not. | Add `Compat.GetItemInfo(itemID)` and `Compat.GetItemCount(itemID)` preferring `C_Item.*` and falling back to the global, then route all five call sites through them. That also closes the review's F-010 (the unguarded `core/TooltipCache.lua:459`) as a side effect, and makes the `.luacheckrc` comment true. |
| **CM-65** | `slash-commands-§1` | MUST | **The slash degradation stub takes down every host verb, not just the schema CLI.** `settings/Slash.lua:340-343`: `function KCM:OnSlashCommand(msg) if not Sl then return printHelp() end …`, and `printHelp` in the lib-absent branch (`:300-303`) prints "…so /cm is unavailable." So with LibKa0s missing, `bar`, `priority`, `stat`, `aio`, `dump`, `resync`, `rewritemacros`, `resetall`, `config`, `version` and `debug` — none of which ever went to the library — all stop answering. `slash-commands-§1` is explicit: "The host verbs never went to the library, so they keep working; what is lost is the schema CLI, and each of those verbs **MUST** name the missing library rather than going quiet." This also produces the user-visible contradiction the 2026-08-05 review logged as F-003: `settings/Panel.lua:265-269` promises "every setting is still reachable with /cm list, /cm get and /cm set" while `/cm set` answers "so /cm is unavailable". | Keep a host-side dispatch in the lib-absent branch: split the verb match out of the library path so `OnSlashCommand` walks `COMMANDS` itself when `Sl` is nil, and let only `list`/`get`/`set`/`reset`/`help` degrade to the honest one-liner. Do **not** re-implement the library's row formatter, parser or `key = value` shape (`slash-commands-§1` forbids it) — a degraded help renders plainly and says so. Then reconcile `settings/Panel.lua:265-269`'s promise with whatever survives. |

## SHOULD

| ID | Section (`filename-§N`) | Sev | Deviation | Fix direction |
|----|-------------------------|-----|-----------|---------------|
| **CM-51** | `library-stack-§4`, `toc-file-§1` | SHOULD | **`AceGUI-3.0` is resolved non-silently, at file load, three times.** `settings/Panel.lua:19`, `settings/StatPriority.lua:23` and `settings/Category.lua:29` all call `LibStub("AceGUI-3.0")` without the `true` flag, so a missing AceGUI **raises at load** in three files. `options-ui-§1` states AceGUI is *survivable, not a dependency*. The five module-level widget files already do it correctly (`modules/KCMIconButton.lua:11` and peers use the guarded form). | Resolve once — `KCM.AceGUI = LibStub("AceGUI-3.0", true)` in the options setup file (CM-46) — and have the three page files read `KCM.AceGUI`, guarding the render paths. `settings/Panel.lua:247` already re-publishes it onto the instance (`UI.AceGUI = AceGUI`), so there is one obvious home. |
| **CM-64** | `testing-§11` | SHOULD | **The addon's own vendor-sync gate passes when it cannot look, and normalizes line endings.** `tests/test_vendor_sync.lua:141,147` — `local tag = siblingTag(); if not tag then return end` — so both cases report PASS with the sibling checkout absent, which is exactly the state this machine is in. The header at `:108-110` claims the skip "is said in the case name"; it is not, and neither case name mentions it. Separately `:135` compares `(here):gsub("\r\n","\n")` against the blob, where `testing-§11` requires raw bytes with **no** normalization. `testing-§11`'s MUSTs are written at the library repo, which is why this is graded SHOULD here — but the failure mode it names ("a gate that goes quiet when it cannot look is worse than no gate") is the one this file has. | Make the absent-sibling path **fail** with a message naming the path it looked for, or rename both cases to carry `(skipped: no sibling checkout)` so `--list` and `docs/test-cases.md` say so out loud. Drop the `gsub` and compare bytes; if the working tree is CRLF-pinned, read the local side in binary and compare against `git show`'s bytes on both sides rather than normalizing one. |

## MAY / advisory

| ID | Section | Sev | Note |
|----|---------|-----|------|
| **CM-52** | `lint` | MAY | `.luacheckrc:23` uses `ignore = { "212", "542", "241" }` — broader than the template's `212/self` + `212/event`, and `241` globally suppresses a real dead-code smell the file's own comment records as a follow-up (`core/TooltipCache.lua`'s `pendingIDs`, write-only at `:147,448,463,480,488`). `docs/automated-tests/RESULTS.md:41-43` already reports the suppression honestly. Narrow to `212/self`, `212/event`, inline-suppress `542`/`241` at their sites — or delete `pendingIDs` and drop `241` entirely. |
| **CM-53** | `STANDARDS.md` reading guide | MAY | Retired global `§N.M` notation survives in ~30 code comments — `core/Bus.lua:1` ("§4.4"), `core/Debug.lua:4` ("§12.5"), `core/Namespace.lua:6` ("§4.1"), `settings/Panel.lua:75,406,507,631,647,665,683,932`, `settings/General.lua:66,103,159`, `settings/Category.lua:182,709`, `settings/MacroBar.lua:35,387`, `modules/MacroManager.lua:10`, `modules/Selector.lua:131`, `.luacheckrc:28`. The scheme is `filename-§N`. Mechanical sweep; several files already use the new form correctly. |
| **CM-54** | `layout-§1` | MAY | **Re-scoped since 2026-08-04.** The two addon files that were on notice are resolved by the CM-47 split (`core/SlashCommands.lua` 1408 → 836; `settings/Panel.lua` 950). What is in the 1000–1500 band now is a **test** file: `tests/test_macrobar.lua` at **1497**, 3 lines under the cap. Already carried with a disposition in `docs/automated-tests/RESULTS.md:96-98`. Split bar / button / flyout at the next case added, not before. |
| **CM-55** | `documentation-§1` | MAY | The README carries an extra `## Credits and bundled libraries` section between `## Troubleshooting` and `## Issues and feature requests` (`README.md:275`). Required sections are all present in the required relative order, so this is an addition rather than a reorder. Note it is load-bearing for CM-64: `tests/test_vendor_sync.lua:100-103` parses the LibKa0s version out of this section's `Bundles [LibKa0s](…) v1.7.0` line (`README.md:277`). Keep it (it is genuinely player-useful and now has a consumer) or move the provenance line first. |
| **CM-56** | `packaging` | MAY | `.pkgmeta`'s ignore list omits `.claude/`, `.superpowers/` and `CHANGELOG.md`, so they would ship to players. The standard names `docs`, `tests`, `_dev`, lockfiles and `.luacheckrc` explicitly, all of which are covered; these three are the same class of dev-only content. `.superpowers/` alone is 60+ files of task briefs and diffs. |
| **CM-57** | `documentation-§4` | MAY | `docs/pending/LEDGER.md` is a LibKa0s-adoption deviation ledger that also functions as a second backlog beside GitHub issues — the drift `documentation-§4` names, though the rule is written against `TODO.md` specifically and no `TODO.md` exists. Migrate the open `LIBKA0S-*` items to issues once the adoption is closed out. |
| **CM-66** | `debug-logging-§1` | MAY | `modules/DebugLog.lua:97-99` says `core/Debug.lua` probes `KCM.DebugLog.AddLine` to decide whether a console exists. It probes `.instance` (`core/Debug.lua:40`). The **behavior** the comment justifies is correct and deliberate — withholding `AddLine` from the stub is a decision, not a gap, and the audit does not flag it — but the comment names the wrong member, so a future reader "restoring symmetry" would break the fallback. One-line comment fix. (Review F-011.) |
| **CM-67** | `preview-mode` | MAY | The macro bar is a positionable display and ships no explicit preview/test mode: unlocking shows the live bar, which is legible only when the user already owns consumables in every managed category. `preview-mode` is SHOULD throughout and the unlocked bar does feed the real render path, so this is a genuine MAY. Consider placeholder rows for empty categories while unlocked. |

---

## Not run — recorded as unverified, not as passes

| Check | Command attempted | Why |
|---|---|---|
| LibKa0s ship-folder drift | `diff -r ../LibKa0s/LibKa0s ./libs/LibKa0s` | The session was pinned to this repo alone; `../LibKa0s` may not be read. The vendored folder's **shape** was inspected instead (ten files, all eight modules in `LibKa0s.xml`, no shell without its attach file) and shows no sign of anti-pattern #48, but byte identity is unproven. |
| testkit drift | `diff -r ../LibKa0s/testkit ./tests/_kit` | Same. The kit is in the right place (`tests/_kit`, never `libs/`) with the expected five files. |

Neither absence is a pass. The addon's own gate for this (`tests/test_vendor_sync.lua`) also went
quiet — see **CM-64**, which is why the gap is invisible from inside the repo too.

---

## Totals

**MUST = 22** (CM-30, CM-34…CM-46, CM-48…CM-50, CM-58…CM-63, CM-65) ·
**SHOULD = 2** (CM-51, CM-64) ·
**MAY/advisory = 7** (CM-52…CM-57, CM-66, CM-67).

Of the 22 MUSTs, **CM-30** is a pre-existing documented deviation with no action expected and
**CM-49** may resolve as an upstream standard correction rather than an addon change. That leaves
**20 actionable MUSTs**. Three of them — **CM-58**, **CM-65** and **CM-63** — are the only ones that
are *live defects a user can hit* rather than shape or documentation gaps; the first two are the
degraded-install path the 2026-08-05 review reproduced end to end.

Six are net new since 2026-08-04 (**CM-58…CM-63**, plus **CM-65**), and of those, three
(**CM-59**, **CM-61**, **CM-62**) are consequences of the standard moving to v2.21.0 rather than of
the addon moving.

## Closed since the last run (2026-08-04, standard v2.17.1)

| ID | Why it is closed |
|----|------------------|
| CM-47 | The dispatcher moved. `settings/Slash.lua` now owns `COMMANDS` (`:65-172`), the `LibKa0s-Slash-1.0` descriptor and instance (`:241-283`), `GetLandingRows` (`:335`) and `KCM:OnSlashCommand` (`:340`); `core/SlashCommands.lua` keeps only the verb bodies, published on `KCM.SlashCommands.Verbs`, and dropped from 1408 to 836 LOC. `slash-commands-§1`'s named file now exists and holds what the section names. |
