# 02 — Deviations

**Addon:** Ka0s Consumable Master · **Prefix:** `CM-` · **Audit:** 2026-09-08 · **Standard:**
**v2.39.0 (2026-09-07)** · **Commit:** `5b01450`

IDs are **stable**. Every ID up to `CM-78` has been used in a prior run; new IDs start at **CM-79**
and retired IDs are never re-purposed. `CM-75` and `CM-77` are carried forward from
`docs/audits/2026-09-07/` because the gap they name still exists; the other nine roots and dependents
from that run are closed and are listed under *Closed since 2026-09-07* below.

**Grading is impact, not rule strength** (`AUDIT.md` step 5). A doc-only or config-only failure is
**Low** even where the rule it fails is a MUST, and each entry names that MUST anyway.

## Tally

| | Count |
|---|---|
| **Headline (root deviations only)** | **6** |
| **Total including `derived from` dependents** | **7** |
| High | 0 |
| Medium | 0 |
| Low | 6 roots (7 with dependents) |
| Info | 0 |
| **MUST failures — roots only** | **3** |
| **MUST failures — including dependents** | **4** |

**Basis of the two numbers.** The headline counts the six root entries in the first table. The total
adds the one `derived from` dependent in the second table. The MUST counts are of entries whose
**Strength** column reads `MUST` — `CM-79`, `CM-81`, `CM-82` as roots, plus `CM-80` as the dependent.

Nothing in this run is reachable by a user, their SavedVariables or their session. There is no High
or Medium row, and that is measured rather than assumed: `luacheck .` is clean over **102** files
with the v2.39.0 lint scope in force, the headless suite is **786/786**, both vendored-payload diffs
against `LibKa0s v1.27.0` are empty, the working tree agrees with the CRLF pin at **0** strays, the
`lizard` run reproduces the newest bundle to the digit, every seam has a degradation branch pinned by
`tests/test_surface_parity.lua`, and the settings panel passes all nine `options-ui` content checks.

---

## Root deviations

| ID | Section (`filename-§N`) | Grade | Strength | Deviation | Fix direction |
|----|-------------------------|-------|----------|-----------|---------------|
| **CM-79** | `localization-§5`, `testing-§12` | Low | **MUST** | **The prose gate's exclusion list is wider than the four categories `localization-§5` allows.** `tests/test_prose.lua:101-109` skips `docs/automated-tests/` and `docs/perf-analysis/` **whole**, but neither directory is entirely frozen: `docs/automated-tests/README.md` is authored prose, `RESULTS.md` is regenerated in place, and `docs/perf-analysis/README.md` is the one file in that store the section itself says *"is rewritten — the bundles are frozen"* (`documentation-§3`). Three authored, rewritten documents are therefore outside a gate whose whole claim is that it covers the authored set — and two British spellings are sitting in one of them today (`CM-80`). The gate's own comment at `:87-100` enumerates the four categories correctly; the code is wider than the comment. | Replace the two directory skips with dated-bundle skips: exclude `docs/automated-tests/<stamp>/` and `docs/perf-analysis/<stamp>/` by pattern-on-the-stamp (or list the bundles), and let the two `README.md` files and `RESULTS.md` back into the scan. Re-run; expect the two `CM-80` hits to redden, and fix them in the same change. |
| **CM-81** | `localization-§5` | Low | **MUST** | **`synchronisation` in authored prose — `docs/settings-panel.md:77`**, in the sentence *"two controls over one piece of session state is a synchronisation problem the design would have invented"*. `-isation` is British; US is `synchronization`. No gate can catch it: `synchronis` is **not** on `localization-§5`'s published `BRITISH` list, and the section forbids a repo adding a private entry (*"A private addition MUST NOT outlive the change that discovered it"*). This is the one hit a whole-repo `-ise/-isation` sweep finds that the list misses. | Two steps, upstream first. (1) Amend `localization-§5`'s `BRITISH` list in `WowAddonStandards` to carry `synchronis` — it passes the admissibility test, since no US word contains it. (2) Re-sync `tests/test_prose.lua`'s copy of both lists whole and fix `docs/settings-panel.md:77`. Fixing the word alone leaves the collection with no gate for it. |
| **CM-82** | `documentation-§5`, anti-pattern #51 | Low | **MUST** | **The over-cap census in `docs/ARCHITECTURE.md` disagrees with the command it publishes, under today's date.** `:39` reads *"Two files, measured 2026-09-08 with"* followed by the exact `git ls-files … wc -l` invocation at `:41`. Run today that command answers `tests/test_settingsui.lua` **1669** and `settings/Panel.lua` **1165**; the table at `:48` records **1528** and the prose at `:53` and `:73` records **1089**. `docs/automated-tests/RESULTS.md:81, 83` — generated by the runner on the same day — carries the correct 1669 and 1165. So the hub disagrees with the generated record beside it while claiming the same measurement date. `tests/test_layout_cap.lua` asserts only table *membership*, so the suite is green over the wrong figures by design. | Re-run the published command and rewrite the four figures (`:48`, `:53`, `:72-73`) from its output. `tests/test_macrobar.lua` at 1904 and `settings/Category.lua` at 1127 are already right. Consider having the census quote `RESULTS.md`'s band table rather than restating it, so there is one number rather than two. |
| **CM-75** | `documentation-§6` | Low | SHOULD | **Bare `§N` continuation citations — 24 sites in 12 files.** Every one is anchored by a full `filename-§N` in the same clause or the adjacent sentence (`options-ui-§8, §18` at `settings/Category.lua:669`, `settings/StatPriority.lua:33`, `tests/test_settingsui.lua:1167`; `debug-logging-§4/§5` at `tests/test_debug.lua:7`; `options-ui-§15 / §16 / §17` at `tests/test_macrobar.lua:1714`; `debug-logging-§5/§8` at `core/ConsumableMaster.lua:58` and `docs/debug.md:36`; six in `docs/module-map.md`, four in `docs/settings-panel.md`, three in `docs/ARCHITECTURE.md`), so none sends a reader to a section that does not exist. **The 2026-09-07 run's MUST half is closed**: the retired dotted `§N.M` form is absent (0 hits) and the nine unanchored code sites `M5-05` was scoped to are all gone. What remains is the SHOULD. Command and scope in `03_EVIDENCE.md`. | Expand each continuation to its full `filename-§N`. `docs/smoke-tests.md`'s own internal numbering (`§2`, `§3c`, `§11a`) is **not** in scope — those name that document's sections — and neither is `modules/Selector.lua:5`'s `TECHNICAL_DESIGN §4`. |
| **CM-77** | `automated-tests-§5` | Low | SHOULD | **Two run bundles still carry no `ANALYSIS.md`** — `docs/automated-tests/20260807-110619/` and `docs/automated-tests/20260825-103407/`. Both `manifest.json` files read `"release": null`, so this is `automated-tests-§5`'s SHOULD, not its release MUST. **Graduated to a root**: its 2026-09-07 parent `CM-72` (stale record) is **closed** — the `20260908-181304` bundle exists and `RESULTS.md` is regenerated from it — and this survives that closure. The cycle's ruling was *fix forward: make the next run write one, and note the gap once* (`01_CONSOLIDATED_FINDINGS.md` § `C08`). The next run **did** write one; the **note was not written**, and the ruling lives in a plan in a thirteenth repository rather than in this repo's register, so nothing here records the decision. | Do not backfill — writing an August analysis today fabricates a record. Write the note: one sentence in `docs/automated-tests/RESULTS.md` (or `README.md`) naming the two bundles and why they have none, or a `## Documented deviations` row against `automated-tests-§5` with a re-check trigger. |
| **CM-83** | `toc-file-§5` | Low | SHOULD | **Conventional groups in the TOC are unmarked.** `toc-file-§5`'s SHOULD asks for a conventional note *once per group*, so the next author knows which lines are free to move. `ConsumableMaster.toc` marks two individual conventional positions (`core\EnvSetup.lua` at `:73-76`, `core\ItemSetup.lua` at `:78-79`) but the `# Modules` block (`:133-143`, ten lines) and `# Locales` (`:38-39`) carry no marker at all, leaving those twelve lines ambiguous between *free* and *not yet understood*. Per the section's own grading this is **one SHOULD row for the file**, never one per line. Related, and deliberately **not** filed as a separate MUST row: `core\CoreSetup.lua`'s comment at `:68-70` names `core\SlashCommands.lua` as the file taking the printer as a file-scope upvalue, but `core/SlashDump.lua:16` takes it too and `core\SlashDump.lua`'s own comment (`:103-106`) names a different constraint — see `03_EVIDENCE.md` for why that is recorded rather than filed. | Two comment lines. Above `# Modules`: *"Conventional — every module here reaches the seams through closures at call time, except modules\MacroBar.lua, whose NS.Perf upvalue is pinned by core\PerfSetup.lua above."* Above `locales\enUS.lua`: *"Conventional within the block; the section header pins it before # Core."* While there, add `core\SlashDump.lua` to CoreSetup's enumeration. |

---

## Derived dependents (excluded from the headline tally)

| ID | Derived from | Section | Grade | Strength | Observation |
|----|--------------|---------|-------|----------|-------------|
| **CM-80** | `derived from CM-79` | `localization-§5`, anti-pattern #46 | Low | **MUST** | **Two British spellings in authored prose** — `docs/perf-analysis/README.md:25` (*"a run **analysed** a week later still sorts"*) and `:26` (*"against its **neighbours**"*). US is `analyzed` and `neighbors`; both `analys` and `neighbour` **are** on `localization-§5`'s published `BRITISH` list, so the only reason the repo's gate is green over them is `CM-79`'s over-wide exclusion. Filed as a dependent rather than a root because narrowing the gate reddens them and both are fixed in that same change; it does not graduate, because its grade is not higher than its root's and no user reads a doc. |

---

## Closed since 2026-09-07

Recorded so the next run can see what moved, and so a reopened gap is visible as a reopening.

| Prior ID | Was | Work item | Verified closed by |
|---|---|---|---|
| `CM-68` | `docs/slash-dispatch.md` absent, map asserted a false *Not applicable* | `M5-04` | The file exists; `docs/ARCHITECTURE.md:331` now reads `Present` with the real trigger (*seventeen verbs … four of them carry a subcommand tree*). |
| `CM-69` | 28 British-spelling sites across 13 files | `M4-13`, `M4c-02` | A full sweep with `localization-§5`'s published lists over the four sanctioned exclusions returns **0** hits in live files. `tests/test_prose.lua` now carries both lists whole (91 / 30) and is green — the sweep it was owed plus the gate it was not. |
| `CM-70` | 7 tracked files disagreed with the CRLF pin | `M4-10` | The `line-endings-§7` counter returns **0**, and `tests/_kit/test_eol.lua` (kit revision 15) now owns the check over the whole tracked set. |
| `CM-71` | Register row cited a rule the standard had changed (`toc-file-§5`) | `M5-02` | Row deleted; the retirement is recorded in prose at `docs/ARCHITECTURE.md:371`, citing `toc-file.md:129` and this repo's own TOC comments. |
| `CM-72` | Watch list stale against the newest bundle and the tree | `M5-01` | Bundle `20260908-181304` exists; today's verbatim `lizard` run reproduces it exactly (18825 NLOC / 1957 functions / 0 warnings). |
| `CM-73` | `tests/test_macrobar.lua` over the 1500 cap and unowned | `M4-14` | Issue [#32](https://github.com/tusharsaxena/ConsumableMaster/issues/32) (`state:triaged`, `bug`) names the two cuts — `layout-§1`'s second terminal state since its 2026-09-08 revision. Issue #33 does the same for `tests/test_settingsui.lua`, which crossed during the cycle. |
| `CM-74` | `RESULTS.md` lead-in named two checkpoints, not three | `M5-01` | `docs/automated-tests/RESULTS.md:14-16` now names the tag gate and CCN 15 explicitly. |
| `CM-75` | 13 unparseable `§N` citations | `M5-05` | **Partially.** The MUST half is closed (0 dotted `§N.M`, 0 unanchored code sites). The SHOULD half survives — see the root table. |
| `CM-76` | `_dev` missing from `.pkgmeta` `ignore:` | `M2-19` | `.pkgmeta:11` carries `- _dev`; and `:13` carries `.pkgmeta`'s own v2.39.0 self-reference, which the weak-form check now also requires. |
| `CM-77` | Two bundles carry no `ANALYSIS.md` | `M5-01` | **Not closed.** Graduated to a root — see above. |
| `CM-78` | `Tests` column carried no `skipped` figure | `M1-LK-07` | `docs/automated-tests/RESULTS.md:22` states the `passed/skipped/total` shape and `:26` reads `786/0/786`. Fixed upstream in the kit and re-vendored. |

---

## Checks run and found compliant (not deviations)

Recorded so a later run does not re-open them. Every claim here has a citation or a command in
`03_EVIDENCE.md`.

- **Vendored payload.** `diff -r <LibKa0s@v1.27.0>/LibKa0s libs/LibKa0s` and
  `diff -r <LibKa0s@v1.27.0>/testkit tests/_kit` are both **empty**, diffed against the tag
  `CLAUDE.md:58` names rather than against the sibling's `HEAD`. Not #45, not #48. Fourteen files,
  ten majors, TOC lists the aggregate XML once. The provenance line is in `CLAUDE.md` and absent from
  `README.md` (not #59, not #58), and the standard badge is bare (`README.md:6`).
- **`.gitattributes`.** Byte-identical to `line-endings-§5`'s canonical client-bound body over all
  81 lines, with no tail — so no `line-endings-§5 appendix` question arises. Working tree agrees with
  the pin: **0**.
- **`.luacheckrc` under the v2.39.0 lint scope.** `exclude_files` narrows to `tests/_kit/` and
  nothing wider; the harness globals sit in `files["tests/"]`, not top-level `read_globals`; there is
  no blanket `ignore`. The `0/0` covers **102** files, test tree included.
- **The AceGUI widget registry.** `core/LSMPatch.lua` is deleted and the re-registration runs through
  `lib.__PatchLSM30Border` from `settings/OptionsSetup.lua:102-140`. Not anti-pattern #76, and a
  whole-repo grep for `RegisterWidgetType` outside `libs/` returns nothing.
- **`MakeCloseButton`.** One wrapper (`core/CoreSetup.lua:185-187`) carrying all three arguments; no
  direct `lib.MakeCloseButton` call site and no `NS.DebugLog.MakeCloseButton`. The addon draws no
  window of its own, so `standalone-windows`' reasoned-decline path is not engaged and its four
  conditions have nothing to test.
- **`options-ui` (a)–(i).** All nine content checks pass, including the v2.39.0 hollow composer
  (`settings/OptionsSetup.lua:250-253`). No broadcast meta row, so §16's exemption is not claimed.
- **`documentation-§3`'s tier model.** Tier 1 complete under the canonical names; every Tier 2
  trigger measured against the code and answered; the `## Documentation map` carries **four** tables
  including `### Verification and record` with exactly its six rows; no non-canonical filename, no
  retired doc, no `docs/perf-runs/`. `ARCHITECTURE.md`'s self-row is present and is neither filed for
  nor against, per v2.39.0.
- **The deviation register, all three `audit-review-history` MUSTs.** Read before filing; no row
  cites a rule the standard has since changed; every re-check trigger evaluated against the tree
  (none fired) and every evidence id resolved (`CM-30`, `CM-63`, `CM-67`, `CM-49` to bundles under
  `docs/audits/`, `CM-R-10` to `docs/reviews/2026-09-07/`). Three rows — `localization-§4`, `compat`,
  `preview-mode` — recorded as **accepted**, not re-filed, and excluded from the MUST tally.
- **A `state:will-not-do` issue with no register row.** All twelve closed declines were read
  (#17-#26, #28, #30, #31). None declines a **standard rule** — they decline optional LibKa0s
  modules, a `MAY` field, and superseded pre-adoption states. No missing register row is owed. No
  `[status]` title prefix (#62) and no `docs/pending/LEDGER.md` (#60).
- **`packaging`, both forms.** Named enumeration complete including `.pkgmeta` itself; strong form
  leaves only `.git`.
- **`architecture-§4`.** Above the threshold and compliant — ten feature modules, event registration,
  and the closed bus at `core/Bus.lua` with per-receiver targets.
- **`localization-§3`.** State 1 (*Routed*): user-facing strings go through `KCM.L`. No English-only
  register row is owed.
- **`performance-§12`.** Does not apply — the harness is wired, `tests/perf.lua` ships and
  `ConsumableMasterPerfDB` is declared. Inside the rule's scope and compliant with it.
- **`library-stack-§1` under v2.39.0.** `AceTimer-3.0` is neither reached nor vendored, which the
  amended *mandatory when used* table makes compliant rather than a gap.
- **`layout-§1`'s cap.** Two breaches, both in `layout-§1`'s second terminal state (an open issue
  naming the seam). An audit **MUST NOT** re-file `layout-§1` against a file the census carries, and
  this run does not — `CM-82` is about the census's arithmetic, not about the breaches.
- **Complexity, watch list and refactor shapes.** 0 functions above CCN 15. Four watch-list entries,
  two reading *Accepted* and both newly in the band at this run (one consecutive run each, against
  anti-pattern #53's threshold of three); the other two read *Owned* with an issue number. No
  `part2`/`doTheRest` helper, no in-function dispatch table, no `t.k = stored.k or D.k` over a field
  whose falsy state is a user choice.
- **Combat lockdown.** The protected macro writers at `modules/MacroManager.lua:309, 320` sit behind
  `InCombatLockdown()` guards at `:435` and `:549`, with deferral to `PLAYER_REGEN_ENABLED`.
- **Shared media.** No private `fonts/`, `icons/` or `textures/` under `media/`; the seam is fed the
  addon's own first vararg; the console carries `addonName`; the perf panel has no `decorate` hook.
  The Blizzard state glyphs are on the **options surface**, which `library-stack-§8` places out of
  scope.
