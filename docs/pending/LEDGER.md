# Pending-item decision ledger

This file is the record of every pending item that has been brought to the user
and decided. It is created and maintained by **`/wow-addon:pending-audit`**,
which sweeps the addon for unfinished work — TODO/FIXME markers, unexecuted
audit and review plan steps, doc open questions, open GitHub issues, and
recorded-but-unacted Claude memory — and interviews each one.

The ledger's job is to make a decision *stick*. Each row is matched on its **ID
plus its evidence hash** (the first 8 chars of `sha1` over the item's verbatim
evidence text). A closed item never resurfaces — unless its evidence text
itself changes, in which case the hash no longer matches and the item is
correctly raised again as something that has moved since you decided it.

## Notation

| Marker | Value | Meaning | Re-surfaces? |
|---|---|---|---|
| 🟢 | `done` | Implemented in the run named by the Date column | No — closed |
| 🔵 | `wont-do` | Deliberately closed; it will never be done | No — closed |
| 🟡 | `deferred` | Not now; still on the books | Yes, as a collapsed count |

Green is resolved, blue is a settled close (declining to do something is a
decision, not a failure), and yellow is the only state still asking for
attention — a column of yellow is this file telling you what is left. There is
deliberately no red: nothing here is an error state.

Both the marker and the word are always written. The word is the data — it is
what `grep wont-do` finds and what a screen reader announces; the marker is
only an affordance for scanning.

## Decisions

| ID | Evidence hash | Source | Decision | Date | Rationale |
|---|---|---|---|---|---|
| PLAN-02 | `9eae021a` | `docs/audits/2026-07-18/05_EXECUTION_PLAN.md` — CM-33 | 🟢 done | 2026-07-31 | Applied the bundle's remediation as written: heading renamed in `CLAUDE.md`, by-name cross-reference added to the first Hard rule in `docs/agent-context.md`. |
| PLAN-03 | `572c9cbc` | `docs/audits/2026-07-18/05_EXECUTION_PLAN.md` — CM-30 | 🟢 done | 2026-07-31 | Closed as confirmed, no change: `docs/scope.md:20`, `docs/agent-context.md:25` and `docs/ARCHITECTURE.md:84` all scope the deviation to TooltipCache text parsing, and classification still keys on numeric `classID`/`subClassID`. |
| PLAN-04 | `6cf64ad0` | `docs/reviews/2026-05-02` — T6.5 / F-021 / LLD-19 | 🟢 done | 2026-07-31 | Fixed ahead of the original trigger rather than waiting for the first numeric dropdown row — cheaper to remove the trap now than to debug it later. Implemented per LLD-19. |
| MEM-01 | `414d58c6` | `memory/MEMORY.md` — augment-rune index line | 🟢 done | 2026-07-31 | Refreshed the entry and closed the two in-game confirmations; user confirmed the behavior was verified in game. Stale test count dropped in favor of the README badge as the single source. |
| PLAN-01 | `b60869ea` | `docs/audits/2026-07-18/05_EXECUTION_PLAN.md` — CM-32 | 🔵 wont-do | 2026-07-31 | User declined both branches of CM-32 — no Wago ID is being added, and no tracked-deviation note is being recorded for it. (Reason not stated; inferred as "not worth documenting either way".) A future standards audit will still flag the missing field. |
| PLAN-06 | `51c75525` | `docs/reviews/2026-05-02` — out-of-scope follow-ups | 🔵 wont-do | 2026-07-31 | The premise is gone: `FireConfigChanged` was deleted by the same review's T3.1 and `core/Bus.lua` is now the addon's real message seam, so there is nothing left to wire up. |
| PLAN-09 | `b04454e7` | `docs/reviews/2026-05-02` — out-of-scope follow-ups | 🔵 wont-do | 2026-07-31 | Self-closing on the review's own terms: `formatNumber` is already O(n) one-pass and the "only revisit if a profile demands" trigger has never fired and is not being watched for. |
| PLAN-05 | `9f00b84c` | `docs/reviews/2026-05-02` — T6.4 / F-020 | 🟡 deferred | 2026-07-31 | Keeping the review's profiling trigger — no measurement has shown the per-render `craftingQualityAtlas` lookup to cost anything. Settled together with PLAN-08, which restates the same change. |
| PLAN-07 | `7b7d4f39` | `docs/reviews/2026-05-02` — out-of-scope follow-ups | 🟡 deferred | 2026-07-31 | `_viewedSpecAuto` already gives the right default silently; surfacing it as a checkbox is discoverability polish, not a fix. |
| PLAN-08 | `6476e9bc` | `docs/reviews/2026-05-02` — out-of-scope follow-ups | 🟡 deferred | 2026-07-31 | Duplicate of PLAN-05 (session-scoped memo for `craftingQualityAtlas`); decided in the same call and carries the same profiling trigger. |
| PLAN-10 | `d6320e6b` | `docs/reviews/2026-05-02` — out-of-scope follow-ups | 🟡 deferred | 2026-07-31 | The discovered-set sweep already runs on every `PLAYER_ENTERING_WORLD`, so a `/cm gc` verb is a debugging convenience rather than a user need. |
| ISS-03 | `f22d2fde` | GitHub issue #3 — Stat Weights | 🟡 deferred | 2026-07-31 | A design project, not a pending-audit call: the weight-sourcing question is unsolved and it changes the ranking model. Ordered priority ranks correctly for the common case today. |
| ISS-04 | `53efa51a` | GitHub issue #4 — Checkbox to enable | 🟡 deferred | 2026-07-31 | Not now. Stays tracked on GitHub; delete-and-re-add remains the workaround for excluding an item. |
| ISS-07 | `c7f1bc72` | GitHub issue #7 — per-slot keybindings | 🟡 deferred | 2026-07-31 | The issue's own two open questions (override bindings vs. `Bindings.xml`, conflict handling) want deciding first; users can bind on a Blizzard bar meanwhile. |
| ISS-08 | `d139aba2` | GitHub issue #8 — cooldown and count styling | 🟡 deferred | 2026-07-31 | Stock Blizzard rendering works and honors the system CVar; better to let the secret-value cooldown rework in `982411c` settle in game before layering styling on that path. |
| ISS-09 | `428b4f30` | GitHub issue #9 — styling for empty slots | 🟡 deferred | 2026-07-31 | Current behavior is functional, just visually flat. Issue #9 already tracks it; no additional issue needed. |
| LIBKA0S-01 | `slash-cli` | LibKa0s adoption — `core/SlashCommands.lua` → `LibKa0s-Slash-1.0` | 🟢 done | 2026-08-01 | Reopened and adopted at the user's direction: the dispatcher, the help header and rows, and the version verb now come from the library, and convergence across the collection was judged worth more than the line count. The size objection below stood but was overruled, and it got cheaper in the process — the mandatory fallback dispatcher was dropped once the user ruled a missing vendored library out of scope. The schema CLI half is still NOT adopted and is still blocked on LIBKA0S-02. Original reasoning follows. |
| ~~LIBKA0S-01~~ | `slash-cli-orig` | superseded — original deferral reasoning | 🔵 wont-do | 2026-07-31 | Adopting the dispatcher would make the file **bigger**: it replaces 19 lines (`printHelp` + `KCM:OnSlashCommand`) and costs ~50 back, because a missing library still needs a hand-written dispatcher — unlike the console, `/cm` is how a user reports the addon is broken, so it cannot simply degrade. The 116-line schema CLI is the part worth deleting, and it is blocked upstream (see LIBKA0S-02). Revisit when it is not. |
| LIBKA0S-04 | `options-makers-3` | LibKa0s adoption — the settings panel's schema-row widget makers | 🟢 done | 2026-08-01 | Adopted: `Helpers.RenderField` and all four makers are the library's. The descriptor gained `colorDecode`/`colorEncode` (colors are stored positionally here, which is what the Ka0s color widget writes) and `sliderCommit = "change"` (the Macro Bar page's number rows drive the bar, so the live drag preview IS the feature). Two rows migrated from this addon's `lsm = "border"` to the library's `dialogControl = "LSM30_Border"`, and the local name map went with the makers. `Helpers.EnumValues` and `LSMValues` stay — they are the addon's own schema vocabulary, called from the rows themselves. ~160 lines out. Prior state follows. |
| ~~LIBKA0S-04~~ | `options-makers-2` | superseded — unblocked, pre-adoption | 🔵 wont-do | 2026-08-01 | **Was: unblocked upstream, not yet adopted.** All three blockers are fixed in OptionsWidgets minor 3 and re-vendored: the dropdown reads the ordered `{value=, text=}` array, `hasAlpha` defaults to true (and `false` is expressible for the first time), and `sliderCommit = "change"` / per-row `commitOn` gives the Macro Bar page its live drag preview back. A fourth gap the ledger never recorded went with them — the makers read `row.desc` where every Ka0s schema declares `tooltip`, which would have blanked every tooltip body silently and only in game. Remaining work is host-side only and is page-file work rather than a library gap: `Helpers.Grid`'s full-width rows have no library equivalent, `RenderRows` auto-emits a Section per group change where the pages already call `Helpers.Section`, and `lsm` → `dialogControl`. Worth ~133 lines. Original evidence follows. |
| ~~LIBKA0S-04~~ | `options-makers` | superseded — original blockers, now fixed | 🔵 wont-do | 2026-07-31 | The panel SHELL is adopted (`d020378`, `e79c0f1` and this change); the row makers are not, and all three reasons are upstream shape mismatches. `OptionsWidgets.lua:249` reads `values` as a key map where our enum rows are ordered arrays of `{value=, text=}` — the same defect as LIBKA0S-02 item 2, one fix unblocks both. `:311` defaults `hasAlpha` to false where ours defaults true, and no row declares it, so all seven color pickers would silently lose their alpha slider. `:222` commits a slider on `OnMouseUp` where ours commits on `OnValueChanged`, with no descriptor hook — live drag preview is the feature on the Macro Bar page's 22 number rows. Worth ~133 more lines when they land. |
| LIBKA0S-05 | `options-registry-3` | LibKa0s adoption — the page registry, refresh trio and panel open | 🟢 done | 2026-08-01 | Adopted: `Helpers.SetRenderer`, `RefreshAllPanels` and `RefreshScalars` are forwarders onto the instance. The five refresh cases in `tests/test_schema.lua` were rewritten to build their ctxs through `Helpers.CreatePanel` + `SetRenderer` rather than hand-assembling one and pushing it onto `KCM.Settings._panels` — the registry is the library's now, but the better reason is that a fabricated ctx could satisfy the refresh contract while a real one did not. Two accepted differences: a failing renderer reports as "settings page '<key>' failed to render" rather than "panel render failed", and the **sidebar** combat notice is the library's `|cffaaaaaa` while `/cm config` keeps this addon's `|cff808080` (`Options.lua` has no `L` seam). Prior state follows. |
| ~~LIBKA0S-05~~ | `options-registry-2` | superseded — unblocked, pre-adoption | 🔵 wont-do | 2026-08-01 | **Was: reopened and unblocked upstream, not yet adopted.** All three regressions are fixed in Options minor 3 and re-vendored: page builders are pcall'd per page and report the failing key, `SetRenderer` carries the combat guard that closes the Settings window (the AddOns sidebar bypasses `OpenOptionsPanel`, so that path had none), and the refresh trio is now two tiers — `RefreshAllPanels` structural, `RefreshScalars` in place — matching this addon's own model. The migration is opt-in: a ctx that never goes through `SetRenderer` keeps the old ungated behavior, so the pages can move one at a time. One thing the addon gives up when it adopts: its combat notice is `|cff808080` where the library's is `|cffaaaaaa`, and `Options.lua` has no `L` seam. Same sentence, different gray. Original evidence follows. |
| ~~LIBKA0S-05~~ | `options-registry` | superseded — original regressions, now fixed | 🔵 wont-do | 2026-07-31 | Three straight regressions, not shape mismatches. `Options.lua:441` runs page builders unguarded where ours pcalls each and names the failing key; its main-panel `OnShow` has no combat guard where ours closes the Settings panel and prints the canonical gray notice (the AddOns-sidebar half of options-ui-§2); and its `RefreshAllPanels` is semantically our `RefreshScalars` minus the `IsShown` gate, the dirty flag and the failure report — same name, opposite meaning, which is why neither is ever forwarded. Our two-tier refresh is strictly richer. Closed rather than deferred. |
| LIBKA0S-02 | `slash-upstream` | `libs/LibKa0s/Slash.lua` — two shape mismatches vs the Ka0s options schema | 🟢 done | 2026-08-01 | Fixed upstream in LibKa0s (Slash minor 4, OptionsWidgets minor 3) and re-vendored. The enum reader takes the ordered `{value=, text=}` array in BOTH majors — the normalizer is duplicated verbatim rather than hoisted into Core, because raising `NEEDS_CORE` would be a breaking change to the vendoring, with a cross-major parity case pinning the two copies in agreement. Colors: `lib.FormatValue` reads the positional shape directly, and `colorDecode`/`colorEncode` join the Slash descriptor under the same names Options already used. Two bonus fixes fell out — a key *set* used to label every dropdown entry `"true"`, and a string row with no values list was un-settable from the CLI. Original evidence follows. |
| ~~LIBKA0S-02~~ | `slash-upstream-orig` | superseded — original evidence | 🔵 wont-do | 2026-07-31 | Blocks the schema-CLI half of LIBKA0S-01; both need fixing in `github.com/tusharsaxena/LibKa0s`, not patched in the vendored copy. (1) `lib.FormatValue:92` reads a color as `v.r/v.g/v.b/v.a` while the standard's own color widget writes `{r, g, b, a}` **positionally** (`settings/Panel.lua:693`), and `kv()` at `Slash.lua:251` calls it directly with no descriptor hook — so all seven color rows would render `{0.00, 0.00, 0.00, 1.00}`, and green, since `tests/test_slash.lua:604` only checks the path appears. (2) `allowedValues:135` returns the `tostring`'d **keys** of `pairs(row.values)`, but enum rows are ordered arrays of `{value=, text=}` (`settings/Panel.lua:566-572`), so `macroBar.orientation` would offer "1, 2" — and `parseNumber:125` has no enum branch at all, regressing commit `6a92e63`. Both verified by reading the source. |
| LIBKA0S-06 | `adoption-tail` | LibKa0s adoption — the four pieces left after LIBKA0S-01/-02/-04/-05 | 🟢 done | 2026-08-01 | All four adopted, two of them only after upstream work. `core/Debug.lua`'s gated sink delegates to the console instance's own `D.Debug`, with the host probe kept because load order forbids capturing the instance (early boot and a degraded install both reach it with nothing to delegate to). `settings/General.lua` takes `D:ConsoleCheckbox()` — its tooltip names `/cm debug` in the same sentence rather than a trailing one, accepted so one description of the console is owned by the console. `Helpers.Grid` and `Helpers.LSMValues` needed the library to grow first: `RenderGrid` (Options/OptionsWidgets minor 4), because `RenderRows` is schema-driven and cannot express a list whose length is not in the schema — the Macro Bar page's per-macro toggles; and a non-empty `LSMValues`, because an empty media list leaves the dropdown unopenable and makes `ValidateSchemaValue` reject even the stored value. A `fallback` field was drafted for DebugLog and dropped: it solved a problem nobody has. |
| LIBKA0S-07 | `rowguard` | `libs/LibKa0s/OptionsWidgets.lua` — `RenderRows` did not pcall each row | 🟢 done | 2026-08-01 | Fixed upstream in OptionsWidgets minor 4 and re-vendored to all three consumers. The library's own `docs/releasing.md` had carried it as a known regression since it replaced KickCD's flow engine, which did guard per row. `RenderGrid` guards its items the same way. Not reachable from this addon before the same release, since `Helpers.Grid` was host code until then. |
| LIBKA0S-08 | `license-record` | adoption report 2026-08-01 — deviations §5, §6 | 🟢 done | 2026-08-01 | Re-vendored the whole folder at LibKa0s v1.1.0 and the only content that moved was the new `libs/LibKa0s/LICENSE` — the library put its MIT notice in the ship payload, so it arrives with the copy rather than as a per-addon step, and `libs/` is not in `.pkgmeta`'s ignore list so it is genuinely in the zip. All eight minors held (Core 2, DebugLog 3, Slash 4, Options 4, OptionsWidgets 4, OptionsScroll 2, Perf 5, PerfPanel 3). The README gained a Credits section naming the tag, because the version this build carries was previously answerable only by grepping eight minor constants out of vendored source — which is the question a re-vendor sweep asks first. Both halves of the new gate are empty here, `--strip-trailing-cr` and bytes alike: this repo's checkout was CRLF throughout while the ship folder was mixed, and the byte half only became meaningful once upstream renormalized. One local instance of the same disease went with it — `tests/harness.lua`'s inventory renderer joined on `\n` where `.gitattributes` pins `*.md text eol=crlf`, so every regeneration of `docs/test-cases.md` wrote the file onto the wrong side of the policy with `git status` staying clean. `tests/_kit/framework.lua` had carried the CRLF write and the note explaining it all along; the host copy had drifted. |
| LIBKA0S-09 | `errstrings-upstream` | `libs/LibKa0s/Slash.lua` — three `L` keys the descriptor cannot reach | 🟡 deferred | 2026-08-01 | `ERR_BOOL`, `ERR_ALLOWED` and `ERR_COLOR` are emitted by `parseBool`, `allowedText` and `parseColor`, which sit ABOVE `lib:New` and therefore read `lib.STRINGS` directly rather than through `Sl:Text`. An instance `L` override cannot reach them, so this addon's three overrides — declared since the adoption, and each pinning wording it had already shipped — have never taken effect: a bad boolean answers the library's "expected true/false/on/off/1/0/yes/no" and a bad enum answers "allowed values:" in lowercase. Nothing is broken, and the four keys that DO route through `Sl:Text` work. Deferred rather than closed because the fix belongs upstream (hoist the parsers onto the instance, or thread the resolver into them) and `libs/` is not this repo's to edit. The three keys stay in `SLASH_STRINGS` as the record of the intended wording, marked dead in place. `tests/test_slashsetup.lua`'s enum case lowercases before matching, which is why it never noticed. |
| LIBKA0S-10 | `ltrap-guards` | adoption report 2026-08-01 — deviations §2 | 🟢 done | 2026-08-01 | Four regression cases, one per adopted major that has user-visible library strings, each asserting a rendered label does not match `^[A-Z][A-Z0-9_]+$`: Perf on all seven `P.STEPS[i].label`, DebugLog on `ConsoleCheckbox()`'s label and tooltip plus the enable acknowledgment, Slash on `HelpHeader()` and `BuildListLines()[1]`, Options on the empty-media placeholder and the render-failure notice. Core gets none and should not: it has no `STRINGS` table and renders nothing of its own — every byte `KCM.Say` emits is the host's. All four assert on what the instance actually rendered and none is guarded on `if label then`, which is the shape the report records as having proved nothing the first time it was tried. What the mutation testing established is worth more than the cases: the trap as originally described **cannot be reproduced against the vendored minors**. Handing every descriptor `KCM.L` leaves all four green, because DebugLog 3 / Slash 4 / Perf 5 resolve through `rawget` and a metatable-only locale table answers nothing to it. What the cases DO catch is the form still reachable — a plain table whose values were read out of `KCM.L` by library key, which is exactly the "the values may come from your locale table" shape the library's own README recommends. That mutation turns every one of them red. |
| LIBKA0S-11 | `usageget-arity` | `core/SlashCommands.lua` — `/cm get` with no path raised | 🟢 done | 2026-08-01 | Found while writing LIBKA0S-10's Slash case. `Sl:CliGet` formats `USAGE_GET` with `(d.slash)` alone; this addon's override carried two `%s`, and `string.format` raises on the missing second argument — so a bare `/cm get`, which is what a user types when they do not know the path, threw a Lua error in game from the day the schema CLI was adopted. Unlike `HELP_ALIAS`, where a surplus placeholder is genuinely dropped, this one is a missing argument. Fixed by spelling the hint's command literally; it is the same `/cm` the descriptor declares twenty lines below. `tests/test_slashsetup.lua` now drives the verb with no argument — nothing else in 549 cases did, which is the whole reason it shipped. |
| LIBKA0S-12 | `reset-convergence` | adoption report 2026-08-01 — deviations §3, recommendations §3 Option A | 🟢 done | 2026-08-01 | The user chose Option A: converge, keep the guard. `/cm reset <path>` is now the library's `Sl:CliReset` — one schema row back to its `default`, path resolved through `findRow`, case preserved — which is what `reset` means in `/at` and `/kcd` and now means here. The confirm-gated global wipe moved to `/cm resetall` **intact**: same `KCM_CONFIRM_RESET` dialog, same wording, same `KCM.ResetAllToDefaults` body, only the verb that raises it changed. That was the whole hazard the adoption prompt named — "re-anchor the popup to `resetall` or the destructive path loses its guard" — and the popup was always registered at file scope and reached by name through the global `StaticPopup_Show`, so the dispatcher swap had never touched it and re-anchoring was a one-line question of which row shows it. `Sl:CliResetAll` is deliberately NOT taken: it walks the schema rows, and this addon's global reset also wipes `categories` and `statPriority`, which the schema does not describe. The break ships loudly rather than silently — `USAGE_RESET` is overridden to say the verb resets one setting and to name `/cm resetall` in gold, so a bare `/cm reset` (what a macro that has lost its argument, or a user going from muscle memory, actually produces) answers with the notice and raises no popup. A new `CHANGELOG.md` carries the breaking entry; this repo had none, and a user-visible break is exactly what one is for. No version bump. The report's finding was that this divergence existed with **zero** occurrences of "reset" anywhere in this ledger; that is what this row closes. Three cases pin it — `resetall` still shows the popup, the popup's `OnAccept` still performs the wipe and says so, and `reset <path>` restores the named row while a neighbor row and a FOOD priority entry both survive. Each was verified red first, and the two obvious wrong moves were mutated in: making `resetall` wipe without asking, and pointing `reset` back at the popup. |
| LIBKA0S-13 | `landing-convergence` | adoption report 2026-08-01 (run v2) — deviations §4, recommendations §5 Option A | 🟢 done | 2026-08-01 | The user chose Option A: converge. The About panel's command rows now come back already rendered by `lib.FormatRow`, through a new `KCM.SlashCommands.GetLandingRows()` that delegates to `Sl:LandingRows()` — the same function `/cm help` has gone through since the adoption. What changed on screen: the spacing either side of the em dash halves from two spaces to one, the dash loses its `|cffffffff` span, and the description gains one. That is the accepted cost the adoption prompt names, and it is what AbsorbTracker, KickCD and BankLedger already look like. **Why this survived a full adoption plus one adoption report**: the panel reaches its rows through `core/SlashCommands.lua` rather than by naming `COMMANDS`, so the 2026-08-01 v1 report's grep of `settings/` for `COMMANDS` returned nothing and recorded this addon as *not applicable* for the convergence — a state it was never in. The v2 report followed the render path instead (`settings/Panel.lua:770` installs `Helpers.BuildAboutContent`; `:718` drew the rows) and found the host format string still there, with the chat half already pinned byte-for-byte against `lib.FormatRow` at `tests/test_slashsetup.lua:57`. Two formatters for one table of data inside one addon is precisely the drift this convergence exists to collapse. The delegation sits behind `GetLandingRows` rather than the panel calling `Sl` directly, so the About page acquires no LibKa0s lookup of its own — the note above `lib:New` asks for that explicitly. With the library absent it returns empty rather than falling back to a host formatter, because `registerPanel` does not register the panel at all in that state, so a fallback would only be the second formatter kept alive under another name. `GetCommandSummary` — the unrendered `{name, desc}` view — was kept at first for callers that wanted the data, then **deleted**: the panel was its last caller in the addon, and an export with no caller is an invitation to format a command row somewhere new. The three suites that read it now read `GetLandingRows` or `COMMANDS` directly, which is one step closer to what ships. One new case pins it, and pins that the chat and panel forms now differ only by the chat indent; the library-repo prompt's "not applicable" worked example was corrected upstream in the same pass, because teaching the next adopter that a helper-function indirection means *not applicable* is how this was missed. |
| LIBKA0S-14 | `canvas-tripwire` | LibKa0s `docs/adoption/2026-08-01-v2/04_RECOMMENDATIONS.md` §6b, §8b | 🟢 done | 2026-08-01 | Two guards in `tests/test_settingsui.lua`, both for things this addon acquired from a re-vendor rather than by asking. **The canvas contract**: Options minor 5 stamps `OnCommit`/`OnRefresh`/`OnDefault` in `CreatePanel`, so the settings pages gained a working Blizzard **footer** Defaults control with no host edit — and nothing here would have noticed losing it again, because the header Defaults button keeps working and looks equivalent to the user. Three cases pin the callbacks, the forwarder reaching a `defaultsOnClick` parked after the panel is built, and the About page's inert-but-callable `OnDefault`; all read through `rawget`, since the frame mock synthesizes a no-op for any PascalCase key. Mutation-verified against `panel.OnDefault = panel.defaultsOnClick`. **The Options tripwire**: not the Core one copied across — `Options.lua` ships its own `lib.STRINGS`, so asserting that table is absent would fail against a module behaving as designed. The source half is what transfers, swept across all three files of the major, with `lib.LAYOUT` pinned beside it because `local L = lib.LAYOUT` there is geometry rather than a locale table. Mutation-verified by adding a `d.L` read. |
| GCD-01 | `da29a5ef` | `modules/MacroBarButton.lua` — GCD-suppress curve, copied from KickCD rather than shared via LibKa0s | 🔵 wont-do | 2026-08-03 | `buildGcdSuppressCurve`/`ApplyCooldown`'s alpha-from-curve logic (and `KCM.GCD_UPPER`, `core/Constants.lua`) is a deliberate line-for-line duplicate of KickCD's `modules/IconGrid_Render.lua` (`buildGcdSuppressCurve` / `applyGcdSuppressionAlpha`) and `core/Constants.lua`'s `Const.GCD_UPPER`. Not hoisted into LibKa0s because this was shipped on a finished branch (`feature/bloodlust-battle-rez`) where a library round-trip was out of scope; the two copies are commented to point at each other so a future LibKa0s adoption pass (in the LIBKA0S-* style already used here) can find both sides. Revisit if a third addon wants the same curve. |
